FROM FROM timescale/timescaledb:latest-pg16

# Switch to root to install packages
USER root

# Ensure apt lists partial directory exists and has correct permissions
RUN mkdir -p /var/lib/apt/lists/partial && chmod 755 /var/lib/apt/lists/partial

# Install dependencies for building pgvector
RUN apt-get update && apt-get install -y --no-install-recommends \
    git \
    build-essential \
    postgresql-server-dev-16 \
 && rm -rf /var/lib/apt/lists/*

# Clone and install pgvector
RUN git clone https://github.com/pgvector/pgvector.git \
 && cd pgvector \
 && make \
 && make install \
 && cd .. \
 && rm -rf pgvector

# Clean up build dependencies
RUN apt-get purge -y --auto-remove git build-essential postgresql-server-dev-16 \
 && apt-get clean \
 && rm -rf /var/lib/apt/lists/*

# Switch back to the default postgres user
USER postgres